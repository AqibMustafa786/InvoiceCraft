rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // EMERGENCY DEBUGGING: GLOBAL ALLOW ALL
    // This confirms if the issue is ANY rule or something else (client/config).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
